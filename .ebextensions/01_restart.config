commands:
  01_restart_app:
    command: |
      # Restart the Node.js application to pick up new environment variables
      if [ -d /var/app/current ]; then
        echo "Restarting application to pick up new environment variables"
        systemctl restart nodejs || true
      fi 