_e5b‍.x([["leaderboard",()=>leaderboard]]);class leaderboard {
    constructor() {
        this.leaderboard = []
    }

    addPair(pair, score) {
        this.leaderboard.push( { pair: pair, score: score } )
    }
}